# 贡献指南

请先描述具体问题或使用场景，再提交范围明确的变更。新增测试应覆盖可复现的问题和有意义的行为。

提交前运行后端 `python -m pytest -q`、前端 `npm test` 和 `npm run build`。使用虚构样例，避免提交真实业务本体、私人路径、日志、密钥或数据库数据。

维护功能的测试必须在临时目录中执行。第三方代码应保留原许可和署名；项目代码采用 Apache-2.0。
